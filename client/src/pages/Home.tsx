import React, { useMemo, useState } from "react";
import { Link } from "wouter";
import { useEffect, useRef } from "react";
import { ArrowRight, Instagram, MapPin, Menu, MessageCircle, Search, ShieldCheck, X } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Label } from "@/components/ui/label";
import { toast } from "sonner";
import { LanguageToggle, useLanguage } from "@/lib/i18n";
import { HomeAccessibleImage, HomeMenuButton } from "@/components/HomeAccessibility";
import { formatProductPrice, formatUsdRate } from "@/lib/currency";
import { VideoShowcase } from "@/components/VideoShowcase";
import { InternationalInquiry } from "@/components/InternationalInquiry";
import { HeroSound, type HeroSoundHandle } from "@/components/HeroSound";
import { HeroGuitarStage } from "@/components/HeroGuitarStage";
import { toneContextForCategory, useAmbientAudio } from "@/components/AmbientAudio";
import { CategoryMotionIcon } from "@/components/CategoryMotionIcon";
import { ToneLogo } from "@/components/ToneLogo";
import { DEFAULT_PRODUCT_IMAGE_ASPECT, getProductImageAspect } from "@/lib/productImageAspect";
import { trackToneEvent } from "@/lib/analytics";

const WHATSAPP = "5565993074442";
const categories = ["Todos", "Guitarras", "Violões", "Pedais", "Amplificadores", "Acessórios"];
const PROMOTION_FILTER = "__promotion__";
const catalogFilters = ["Todos", PROMOTION_FILTER, ...categories.slice(1)];
const PROMOTION_BANNER = "/media/tone-promotion-limited-time-banner_05c016b6.png";
const CUSTOM_SIGNATURE_IMAGES = {
  front: "/media/tone-custom-atelier-front-complete_5e584e60.png",
  back: "/media/tone-custom-atelier-back-complete_fed61d0f.png",
} as const;
const fallbackImages = ["https://images.unsplash.com/photo-1550291652-6ea9114a47b1?auto=format&fit=crop&w=900&q=85", "https://images.unsplash.com/photo-1525201548942-d8732f6617a0?auto=format&fit=crop&w=900&q=85", "https://images.unsplash.com/photo-1510915361894-db8b60106cb1?auto=format&fit=crop&w=900&q=85"];

function CatalogProductImage({ src, alt, children }: { src: string; alt: string; children: React.ReactNode }) {
  const [aspect, setAspect] = useState(DEFAULT_PRODUCT_IMAGE_ASPECT);
  return <div className="relative overflow-hidden bg-[#101012] transition-[aspect-ratio] duration-300" style={{ aspectRatio: aspect }}>
    <img src={src} alt={alt} onLoad={event => setAspect(current => {
      const next = getProductImageAspect(event.currentTarget.naturalWidth, event.currentTarget.naturalHeight);
      return current === next ? current : next;
    })} className="h-full w-full object-contain" />
    {children}
  </div>;
}

function whatsapp(message: string) { trackToneEvent({ eventType: "whatsapp_contact", source: "home" }); window.open(`https://wa.me/${WHATSAPP}?text=${encodeURIComponent(message)}`, "_blank", "noopener,noreferrer"); }
export default function Home() {
  const { t, language } = useLanguage();
  const [category, setCategory] = useState("Todos");
  const [search, setSearch] = useState("");
  const [mobileMenu, setMobileMenu] = useState(false);
  const [heroSoundPlaying, setHeroSoundPlaying] = useState(false);
  const [heroSoundMuted, setHeroSoundMuted] = useState(false);
  const [customView, setCustomView] = useState<"front" | "back">("front");
  const heroSoundRef = useRef<HeroSoundHandle>(null);
  const { setToneContext } = useAmbientAudio();
  const [trade, setTrade] = useState({ name: "", contact: "", instrument: "", message: "" });
  const { data: products = [], isLoading } = trpc.products.list.useQuery({ category: category === "Todos" || category === PROMOTION_FILTER ? undefined : category, promotionOnly: category === PROMOTION_FILTER || undefined, search: search || undefined });
  const { data: soldProducts = [] } = trpc.products.list.useQuery({ soldOnly: true });
  const { data: fxQuote, isLoading: isFxLoading } = trpc.currency.usdQuote.useQuery();
  const featured = useMemo(() => products.slice(0, 3), [products]);
  useEffect(() => { setToneContext(toneContextForCategory(category === "Todos" || category === PROMOTION_FILTER ? "Guitarras" : category)); }, [category, setToneContext]);
  useEffect(() => {
    const sections: Array<[string, ReturnType<typeof toneContextForCategory>]> = [["custom", "guitar"], ["catalogo", toneContextForCategory(category === "Todos" || category === PROMOTION_FILTER ? "Guitarras" : category)], ["sobre", "neutral"], ["videos", "effects"], ["international", "acoustic"], ["negociar", "guitar"]];
    const observer = new IntersectionObserver(entries => { const focused = entries.filter(entry => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0]; const found = sections.find(([id]) => id === focused?.target.id); if (found) setToneContext(found[1]); }, { threshold: [0.35, 0.55, 0.75] });
    sections.forEach(([id]) => { const element = document.getElementById(id); if (element) observer.observe(element); });
    return () => observer.disconnect();
  }, [category, setToneContext]);

  useEffect(() => {
    const images = Array.from(document.querySelectorAll<HTMLImageElement>("#custom img[alt*='vista'], #custom img[alt*='view']"));
    const openZoom = (source: string, description: string) => {
      const previousFocus = document.activeElement as HTMLElement | null;
      const overlay = document.createElement("div");
      overlay.className = "tone-custom-zoom-overlay";
      const dialog = document.createElement("div");
      dialog.className = "tone-custom-zoom-dialog";
      dialog.setAttribute("role", "dialog");
      dialog.setAttribute("aria-modal", "true");
      dialog.setAttribute("aria-label", language === "en" ? "Expanded Custom guitar image" : "Imagem ampliada da guitarra Custom");
      const close = document.createElement("button");
      close.type = "button";
      close.className = "tone-custom-zoom-close";
      close.textContent = language === "en" ? "Close" : "Fechar";
      close.setAttribute("aria-label", language === "en" ? "Close expanded image" : "Fechar imagem ampliada");
      const image = document.createElement("img");
      image.src = source;
      image.alt = description;
      image.className = "tone-custom-zoom-image";
      dialog.append(close, image);
      overlay.append(dialog);
      const dismiss = () => { document.removeEventListener("keydown", onKeyDown); overlay.remove(); previousFocus?.focus(); };
      const onKeyDown = (event: KeyboardEvent) => { if (event.key === "Escape") dismiss(); };
      close.addEventListener("click", dismiss);
      overlay.addEventListener("click", event => { if (event.target === overlay) dismiss(); });
      document.addEventListener("keydown", onKeyDown);
      document.body.append(overlay);
      close.focus();
    };
    const handlers = images.map(image => {
      image.tabIndex = 0;
      image.setAttribute("role", "button");
      image.setAttribute("aria-label", language === "en" ? "Open expanded Custom guitar image" : "Abrir imagem ampliada da guitarra Custom");
      image.title = language === "en" ? "Click to zoom" : "Clique para ampliar";
      const activate = () => openZoom(image.currentSrc || image.src, image.alt);
      const onKeyDown = (event: KeyboardEvent) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); activate(); } };
      image.addEventListener("click", activate);
      image.addEventListener("keydown", onKeyDown);
      return () => { image.removeEventListener("click", activate); image.removeEventListener("keydown", onKeyDown); };
    });
    return () => handlers.forEach(remove => remove());
  }, [customView, language]);

  const submitTrade = (event: React.FormEvent) => {
    event.preventDefault();
    if (!trade.name || !trade.contact || !trade.instrument) return toast.error("Preencha nome, contato e instrumento.");
    whatsapp(`Olá! Quero negociar um instrumento com a Tone Marketing.\n\nNome: ${trade.name}\nContato: ${trade.contact}\nInstrumento: ${trade.instrument}\nMensagem: ${trade.message || "Sem mensagem adicional."}`);
  };

  return <div className="min-h-screen overflow-x-hidden bg-[#0b0b0c] text-white">
    <div className="pointer-events-none fixed inset-0 -z-0 opacity-40 [background-image:radial-gradient(circle_at_20%_10%,rgba(220,38,38,.18),transparent_32%),radial-gradient(circle_at_80%_30%,rgba(255,255,255,.06),transparent_22%)]" />
    <header className="sticky top-0 z-40 border-b border-white/10 bg-[#0b0b0c]/85 backdrop-blur-xl">
      <div className="container flex h-20 items-center justify-between">
        <ToneLogo language={language} />
        <nav className="hidden items-center gap-6 text-sm text-white/65 xl:flex"><a href="#catalogo" className="transition hover:text-white">{t("Catálogo")}</a><a href="#custom" className="transition hover:text-white">Custom</a><a href="#vendidos" className="transition hover:text-white">{language === "en" ? "Sold" : "Vendidos"}</a><a href="#videos" className="transition hover:text-white">{language === "en" ? "Videos" : "Vídeos"}</a><Link href="/envio-internacional" className="transition hover:text-white">{language === "en" ? "Shipping" : "Envio"}</Link><Link href="/confianca" className="transition hover:text-white">{language === "en" ? "Trust" : "Confiança"}</Link><a href="#negociar" className="transition hover:text-white">{t("Negocie seu instrumento")}</a><a href="#sobre" className="transition hover:text-white">{t("Sobre")}</a></nav>
        <div className="hidden items-center gap-3 xl:flex"><LanguageToggle/><a href="https://instagram.com/tonemarketgt" target="_blank" rel="noreferrer" className="rounded-full border border-white/10 p-2 text-white/70 hover:border-white/25 hover:text-white"><Instagram size={17}/></a><Button onClick={() => whatsapp(t("Olá! Vim pelo site da Tone Marketing e gostaria de falar com vocês."))} className="rounded-full bg-white px-5 text-black hover:bg-red-500 hover:text-white">{t("Falar no WhatsApp")} <ArrowRight size={16}/></Button></div>
        <HomeMenuButton label={t("Abrir menu")} open={mobileMenu} onClick={() => setMobileMenu(!mobileMenu)}>{mobileMenu ? <X/> : <Menu/>}</HomeMenuButton>
      </div>
      {mobileMenu && <div className="border-t border-white/10 px-5 py-4 xl:hidden"><div className="flex flex-col gap-4 text-sm text-white/70"><LanguageToggle/><a href="#catalogo" onClick={() => setMobileMenu(false)}>{t("Catálogo")}</a><a href="#custom" onClick={() => setMobileMenu(false)}>Custom</a><a href="#vendidos" onClick={() => setMobileMenu(false)}>{language === "en" ? "Sold" : "Vendidos"}</a><a href="#videos" onClick={() => setMobileMenu(false)}>{language === "en" ? "Videos" : "Vídeos"}</a><Link href="/envio-internacional" onClick={() => setMobileMenu(false)}>{language === "en" ? "Shipping guide" : "Guia de envio"}</Link><Link href="/confianca" onClick={() => setMobileMenu(false)}>{language === "en" ? "Trust standard" : "Padrão de confiança"}</Link><a href="#negociar" onClick={() => setMobileMenu(false)}>{t("Negocie seu instrumento")}</a><a href="#sobre" onClick={() => setMobileMenu(false)}>{t("Sobre")}</a><a href="#contato" onClick={() => setMobileMenu(false)}>{t("Contato")}</a><Button onClick={() => whatsapp(t("Olá! Vim pelo site da Tone Marketing."))} className="bg-red-600">{t("Falar no WhatsApp")}</Button></div></div>}
    </header>

    <main id="top" className="relative z-10">
      <section className="relative min-h-[760px] overflow-hidden border-b border-white/10 sm:min-h-[850px] lg:min-h-[700px]">
        <HeroGuitarStage playing={heroSoundPlaying} muted={heroSoundMuted} language={language} onActivate={() => heroSoundRef.current?.start()}/>
        <div className="container relative z-10 flex min-h-[760px] items-end py-12 sm:min-h-[850px] sm:py-16 lg:min-h-[700px] lg:items-center lg:py-24">
          <div className="w-full max-w-3xl pt-[20rem] sm:pt-[26rem] lg:pt-0"><div data-testid="tone-signal-signature" aria-hidden="true" className="mb-5 flex h-8 items-center gap-1.5 sm:mb-6"><span className="mr-3 h-px w-12 bg-red-500/70"/>{[10, 20, 14, 28, 18, 24, 12].map((height, index) => <span key={index} className={`w-1 rounded-full ${index === 3 ? "bg-red-500" : "bg-white/35"}`} style={{ height }}/>)}</div><HeroSound ref={heroSoundRef} language={language} onPlayingChange={setHeroSoundPlaying} onMutedChange={setHeroSoundMuted}/><h1 className="max-w-3xl text-4xl font-black leading-[.96] tracking-[-.06em] sm:text-6xl xl:text-7xl">{t("Seu próximo som começa aqui.").split(" ").map((word, index) => <span key={`${word}-${index}`} className={word.toLowerCase().includes("som") || word.toLowerCase().includes("sound") ? "text-red-500" : ""}>{word} </span>)}</h1><p className="mt-6 max-w-xl text-base leading-7 text-white/65 sm:mt-7 sm:text-lg sm:leading-8">{t("Instrumentos selecionados, equipamentos com personalidade e atendimento de quem realmente entende de música.")}</p><div className="mt-8 flex flex-col gap-3 sm:mt-9 sm:flex-row"><a href="#catalogo"><Button className="h-12 rounded-full bg-red-600 px-7 text-base hover:bg-red-500">{t("Explorar catálogo")} <ArrowRight size={17}/></Button></a><a href="#negociar"><Button variant="outline" className="h-12 rounded-full border-white/15 px-7 text-base text-white hover:bg-white/10">{t("Quero negociar")}</Button></a></div><div className="mt-9 flex gap-8 border-t border-white/10 pt-5 text-sm sm:mt-12 sm:pt-6"><div><strong className="block text-2xl">637+</strong><span className="text-white/45">{t("itens selecionados")}</span></div><div><strong className="block text-2xl">Cuiabá</strong><span className="text-white/45">{t("MT • atendimento próximo")}</span></div></div></div>
        </div>
      </section>

      <section className="border-y border-white/10 bg-white/[.025] py-8"><div className="container grid grid-cols-2 gap-3 sm:grid-cols-3 xl:grid-cols-5">{categories.slice(1).map(item => <button key={item} onClick={() => { setCategory(item); document.getElementById("catalogo")?.scrollIntoView({ behavior: "smooth" }); }} className="group overflow-hidden rounded-2xl border border-white/10 bg-[#101012] text-left transition hover:-translate-y-1 hover:border-red-500/50 hover:bg-[#151013]"><CategoryMotionIcon category={item}/><div className="px-4 pb-4 pt-3 sm:px-5 sm:pb-5"><span className="block text-sm font-bold">{t(item)}</span><span className="mt-1 block text-xs text-white/40">{t("Explorar seleção")}</span></div></button>)}</div></section>

      <section id="custom" className="container scroll-mt-24 py-24"><div className="grid overflow-hidden rounded-[2rem] border border-amber-300/20 bg-[radial-gradient(circle_at_80%_12%,rgba(245,158,11,.14),transparent_35%),linear-gradient(130deg,rgba(76,5,25,.35),rgba(11,11,12,.95)_55%)] lg:grid-cols-[.92fr_1.08fr] lg:items-stretch"><div className="relative flex min-h-[28rem] flex-col overflow-hidden border-b border-amber-300/15 bg-[#120e11] lg:min-h-[35rem] lg:border-b-0 lg:border-r"><div className="relative min-h-0 flex-1 overflow-hidden"><img src={CUSTOM_SIGNATURE_IMAGES[customView]} alt={language === "en" ? `Premium Custom signature electric guitar, ${customView} view` : `Guitarra Custom Signature premium, vista ${customView === "front" ? "frontal" : "traseira"}`} className="absolute inset-0 h-full w-full object-cover object-center transition duration-700 hover:scale-[1.025]"/><div className="absolute inset-0 bg-gradient-to-t from-black/55 via-transparent to-transparent"/><span className="absolute left-5 top-5 rounded-full border border-amber-200/30 bg-black/55 px-3 py-1 text-[10px] font-black uppercase tracking-[.18em] text-amber-100">Custom</span><span className="absolute bottom-4 left-5 rounded-full border border-white/15 bg-black/55 px-3 py-1 text-[10px] font-bold uppercase tracking-[.16em] text-white/80">{customView === "front" ? (language === "en" ? "Front view" : "Vista frontal") : (language === "en" ? "Back view" : "Vista traseira")}</span></div><div className="grid grid-cols-2 gap-2 border-t border-white/10 bg-black/35 p-3"><button type="button" onClick={() => setCustomView("front")} className={`overflow-hidden rounded-xl border text-left transition ${customView === "front" ? "border-amber-300 ring-1 ring-amber-300" : "border-white/10 opacity-55 hover:opacity-100"}`} aria-pressed={customView === "front"}><img src={CUSTOM_SIGNATURE_IMAGES.front} alt="" className="h-16 w-full object-cover object-center"/><span className="block px-2 py-1.5 text-[10px] font-bold text-white/75">{language === "en" ? "Front" : "Frente"}</span></button><button type="button" onClick={() => setCustomView("back")} className={`overflow-hidden rounded-xl border text-left transition ${customView === "back" ? "border-amber-300 ring-1 ring-amber-300" : "border-white/10 opacity-55 hover:opacity-100"}`} aria-pressed={customView === "back"}><img src={CUSTOM_SIGNATURE_IMAGES.back} alt="" className="h-16 w-full object-cover object-center"/><span className="block px-2 py-1.5 text-[10px] font-bold text-white/75">{language === "en" ? "Back" : "Costas"}</span></button></div></div><div className="flex flex-col justify-center p-7 sm:p-10 lg:p-14"><p className="text-xs font-black uppercase tracking-[.28em] text-amber-300">Custom</p><h2 className="mt-4 max-w-xl text-4xl font-black leading-[.98] tracking-[-.05em] sm:text-5xl">{language === "en" ? "Custom No. 01 · Signature" : "Custom Nº 01 · Signature"}</h2><p className="mt-5 max-w-xl text-base leading-7 text-white/65">{language === "en" ? "Your Custom begins with your own idea. It can come from a sound you are chasing, a reference photo, a favorite neck feel or simply the desire to build a guitar that does not exist yet. This Signature is only a visual starting point; the final instrument is specified around you." : "A sua Custom começa com a sua própria ideia. Ela pode nascer de um timbre que você procura, de uma foto de referência, da sensação de braço que você gosta ou simplesmente da vontade de criar uma guitarra que ainda não existe. Esta Signature é apenas um ponto de partida visual; o instrumento final é especificado em torno de você."}</p><div className="mt-5 rounded-2xl border border-amber-200/15 bg-black/20 p-4 text-sm leading-6 text-amber-50/75">{language === "en" ? "Send your photos, sketches, inspirations or technical notes directly on WhatsApp. Together we define body shape, woods, neck profile, scale, radius, frets, pickups, electronics, hardware and finish before the project moves forward." : "Envie fotos, rascunhos, inspirações ou observações técnicas diretamente pelo WhatsApp. Juntos definimos formato do corpo, madeiras, perfil do braço, escala, raio, trastes, captadores, elétrica, ferragens e acabamento antes de o projeto avançar."}</div><div className="mt-7 flex flex-wrap gap-2">{(language === "en" ? ["Your visual reference", "Pickup voicing by request", "Neck feel chosen by you", "Configurable hardware and finish"] : ["Sua referência visual", "Captação sob medida", "Sensação de braço escolhida", "Ferragens e acabamento configuráveis"]).map(item => <span key={item} className="rounded-full border border-white/10 bg-black/25 px-3 py-1.5 text-xs font-semibold text-white/70">{item}</span>)}</div><div className="mt-9 flex flex-col gap-3 sm:flex-row"><Button onClick={() => whatsapp(language === "en" ? "Hello! I would like to begin a Custom guitar project with Tone.\n\nI will send my photos, inspirations and references so we can define the body shape, woods, neck, pickups, electronics, hardware, finish, timeline and shipping." : "Olá! Quero iniciar um projeto de guitarra Custom com a Tone.\n\nVou enviar minhas fotos, inspirações e referências para definirmos formato do corpo, madeiras, braço, captadores, elétrica, ferragens, acabamento, prazo e envio.")} className="h-12 rounded-full bg-amber-300 px-6 font-bold text-black hover:bg-white"><MessageCircle size={18}/>{language === "en" ? "Share your Custom idea" : "Enviar minha ideia Custom"}</Button><a href="#negociar"><Button variant="outline" className="h-12 rounded-full border-white/15 px-6 text-white hover:bg-white/10">{language === "en" ? "Talk to a specialist" : "Falar com um especialista"} <ArrowRight size={17}/></Button></a></div><p className="mt-5 text-xs leading-5 text-white/40">{language === "en" ? "Each Custom is quoted individually after the project conversation. Specifications, construction window, shipping and payment are confirmed directly on WhatsApp." : "Cada Custom é cotada individualmente após a conversa sobre o projeto. Especificações, janela de construção, envio e pagamento são confirmados diretamente pelo WhatsApp."}</p></div></div></section>

      <section id="catalogo" className="container scroll-mt-24 py-24"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-red-400">{t("No radar da Tone")}</p><h2 className="mt-3 text-4xl font-black tracking-[-.04em] sm:text-5xl">{t("Catálogo em destaque")}</h2><p className="mt-3 max-w-xl text-white/50">{t("Escolha seu instrumento e fale direto com a loja para confirmar detalhes, pagamento e retirada.")}</p>{language === "en" && <p className="mt-3 text-xs text-white/40">{fxQuote?.rate ? `USD estimates · ${formatUsdRate(fxQuote.rate)} · ${fxQuote.source}` : isFxLoading ? "Loading USD estimate…" : "USD estimate unavailable. Confirm the final price on WhatsApp."}</p>}</div><div className="relative w-full md:w-72"><Search className="absolute left-3 top-3 text-white/35" size={18}/><Input value={search} onChange={e => setSearch(e.target.value)} placeholder={t("Buscar instrumento...")} className="h-11 rounded-xl border-white/10 bg-white/5 pl-10 text-white placeholder:text-white/30"/></div></div><button type="button" onClick={() => setCategory(PROMOTION_FILTER)} className="group relative mt-8 block w-full overflow-hidden rounded-3xl border border-amber-300/25 bg-[#160e0a] text-left shadow-[0_18px_60px_rgba(220,38,38,.12)] transition duration-300 hover:-translate-y-0.5 hover:border-amber-300/55 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-300"><img src={PROMOTION_BANNER} alt={language === "en" ? "Limited-time Tone Market promotion" : "Promoção por tempo limitado da Tone Market"} className="h-auto w-full object-cover opacity-75 transition duration-500 group-hover:scale-[1.015] group-hover:opacity-95"/><div className="absolute inset-0 bg-gradient-to-r from-black/75 via-black/15 to-transparent"/><div className="absolute inset-x-5 bottom-5 flex items-end justify-between gap-4 sm:inset-x-8 sm:bottom-8"><div className="max-w-md"><p className="text-[10px] font-black uppercase tracking-[.24em] text-amber-200">{language === "en" ? "Limited-time deal" : "Promoção por tempo limitado"}</p><p className="mt-2 text-sm font-semibold leading-5 text-white sm:text-base">{language === "en" ? "Select offers can leave the catalog at any moment." : "Ofertas selecionadas podem sair do catálogo a qualquer momento."}</p></div><span className="shrink-0 rounded-full bg-amber-300 px-4 py-2 text-xs font-black text-black transition group-hover:bg-white">{language === "en" ? "View deals" : "Ver ofertas"}</span></div></button><div className="mt-8 flex gap-2 overflow-x-auto pb-2">{catalogFilters.map(item => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm transition ${category === item ? "bg-red-600 text-white" : "border border-white/10 text-white/55 hover:text-white"}`}>{item === PROMOTION_FILTER ? (language === "en" ? "Limited-time deals" : "Promoção por tempo limitado") : t(item)}</button>)}</div>{isLoading ? <div className="mt-10 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3"><div className="h-96 animate-pulse rounded-3xl bg-white/5"/><div className="h-96 animate-pulse rounded-3xl bg-white/5"/><div className="h-96 animate-pulse rounded-3xl bg-white/5"/></div> : products.length === 0 ? <div className="mt-10 rounded-3xl border border-dashed border-white/15 p-14 text-center text-white/50">{category === PROMOTION_FILTER ? (language === "en" ? "No limited-time deals are active right now." : "Nenhuma promoção por tempo limitado está ativa agora.") : t("Ainda não há produtos publicados nesta categoria. Cadastre o primeiro item pela área administrativa.")}</div> : <div className="mt-10 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map((product, i) => <Link key={product.id} href={`/produto/${product.slug}`}><article className="group overflow-hidden rounded-3xl border border-white/10 bg-white/[.045] transition duration-300 hover:-translate-y-1 hover:border-red-500/45 hover:bg-white/[.07]"><CatalogProductImage src={product.images?.[0]?.url || fallbackImages[i % fallbackImages.length]} alt={product.name}><div className="absolute left-4 top-4 rounded-full bg-black/60 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-white/75">{product.condition === "new" ? t("Novo") : t("Usado")}</div>{product.promotionActive ? <div className="absolute left-4 top-12 rounded-full bg-amber-400 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-black">{language === "en" ? "Limited deal" : "Promoção"}</div> : null}<div className="absolute bottom-4 left-4 rounded-full bg-red-600 px-3 py-1 text-[10px] font-bold uppercase tracking-wider">{t(product.category)}</div><div className={`absolute right-4 top-4 rounded-full px-3 py-1 text-[10px] font-bold uppercase tracking-wider ${product.available ? "bg-emerald-500/80" : "bg-black/65 text-white/60"}`}>{product.available ? t("Disponível") : t("Indisponível")}</div></CatalogProductImage><div className="p-5"><h3 className="text-lg font-bold">{product.name}</h3><div className="mt-3 flex items-center justify-between gap-3"><span className="font-semibold text-white/75">{formatProductPrice(product.priceCents, language, fxQuote?.rate, t("Consulte no WhatsApp"), isFxLoading ? "USD estimate loading…" : "USD estimate unavailable")}</span><span className="text-xs font-bold text-red-400 transition group-hover:text-red-300">{t("Ver detalhes →")}</span></div></div></article></Link>)}</div>}</section>

      <section id="vendidos" className="container scroll-mt-24 pb-24"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-violet-300">{language === "en" ? "Tone archive" : "Arquivo Tone"}</p><h2 className="mt-3 text-4xl font-black tracking-[-.04em] sm:text-5xl">{language === "en" ? "Sold" : "Vendidos"}</h2><p className="mt-3 max-w-2xl text-white/50">{language === "en" ? "Instruments that have already passed through Tone Market. Open an item to see its complete history and use it as a reference for a custom order." : "Instrumentos que já passaram pela Tone Market. Abra um item para ver seu histórico completo e usá-lo como referência para uma encomenda."}</p></div><a href="#negociar" className="inline-flex w-fit items-center gap-2 text-sm font-bold text-violet-200 transition hover:text-white">{language === "en" ? "Looking for a similar instrument?" : "Procura um instrumento parecido?"} <ArrowRight size={16}/></a></div>{soldProducts.length ? <div className="mt-10 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3">{soldProducts.map((product, i) => <Link key={product.id} href={`/produto/${product.slug}`}><article className="group overflow-hidden rounded-3xl border border-violet-300/15 bg-violet-400/[.035] transition duration-300 hover:-translate-y-1 hover:border-violet-300/45 hover:bg-violet-400/[.065]"><CatalogProductImage src={product.images?.[0]?.url || fallbackImages[i % fallbackImages.length]} alt={product.name}><div className="absolute left-4 top-4 rounded-full bg-violet-300 px-3 py-1 text-[10px] font-black uppercase tracking-wider text-[#1a1022]">{language === "en" ? "Sold" : "Vendido"}</div><div className="absolute bottom-4 left-4 rounded-full bg-black/70 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-white/85">{t(product.category)}</div></CatalogProductImage><div className="p-5"><h3 className="text-lg font-bold">{product.name}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-white/50">{product.description}</p><div className="mt-4 flex items-center justify-between gap-3"><span className="text-xs font-bold text-violet-200">{language === "en" ? "View history" : "Ver histórico"}</span><span className="text-xs font-bold text-violet-200 transition group-hover:text-white">{language === "en" ? "Place an order →" : "Fazer encomenda →"}</span></div></div></article></Link>)}</div> : <div className="mt-8 rounded-3xl border border-dashed border-violet-300/20 bg-violet-400/[.025] p-10 text-center text-white/45">{language === "en" ? "The first instruments in the Tone archive will appear here." : "Os primeiros instrumentos do arquivo Tone aparecerão aqui."}</div>}</section>
      {featured.length > 0 && <section className="container pb-20"><div className="grid gap-5 rounded-3xl border border-red-500/20 bg-gradient-to-br from-red-600/15 to-transparent p-7 md:grid-cols-[1fr_auto] md:items-center md:p-10"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-red-300">{t("Atendimento humano")}</p><h2 className="mt-3 text-3xl font-black tracking-tight">{t("Quer ouvir uma recomendação?")}</h2><p className="mt-2 max-w-xl text-white/55">{t("Conte o que você toca, seu estilo e seu orçamento. A gente monta uma seleção que faça sentido para você.")}</p></div><Button onClick={() => whatsapp(language === "en" ? "Hello! I would like an instrument recommendation. My style is: " : "Olá! Quero uma recomendação de instrumento. Meu estilo é: ")} className="rounded-full bg-white px-6 text-black hover:bg-red-500 hover:text-white">{t("Conversar com a Tone")} <MessageCircle size={17}/></Button></div></section>}

      <section id="sobre" className="container scroll-mt-24 pb-24"><div className="grid gap-8 overflow-hidden rounded-3xl border border-white/10 bg-white/[.045] p-6 sm:p-8 md:grid-cols-[.75fr_1.25fr] md:items-center"><div className="relative aspect-[4/3] overflow-hidden rounded-2xl bg-black"><HomeAccessibleImage src="/media/thiago-tone-owner_28d3c221.jpg" alt={t("Thiago, proprietário da Tone Marketing, em sua loja de instrumentos")} className="h-full w-full object-cover object-center opacity-90"/><div className="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"/></div><div><p className="text-xs font-bold uppercase tracking-[.25em] text-red-400">{t("Sobre a Tone")}</p><h2 className="mt-3 text-3xl font-black tracking-[-.04em] sm:text-4xl">{t("Música boa começa com conversa boa.")}</h2><p className="mt-4 max-w-2xl leading-7 text-white/55">{t("A Tone Marketing nasceu em Cuiabá para aproximar músicos de instrumentos que realmente fazem sentido para seu som. Aqui, cada peça passa por uma curadoria próxima, com transparência e atendimento de músico para músico.")}</p><p className="mt-4 text-sm font-semibold text-white/75">Thiago • Tone Marketing</p><a href="https://instagram.com/tonemarketgt" target="_blank" rel="noreferrer" className="mt-5 inline-flex text-sm font-bold text-red-300 hover:text-red-200">{t("Conheça o dia a dia no Instagram →")}</a></div></div></section><VideoShowcase language={language}/><InternationalInquiry language={language}/><section id="negociar" className="container scroll-mt-24 py-24"><div className="grid gap-12 lg:grid-cols-[.85fr_1.15fr] lg:items-center"><div><p className="text-xs font-bold uppercase tracking-[.25em] text-red-400">{t("Faça seu instrumento circular")}</p><h2 className="mt-3 text-4xl font-black tracking-[-.04em] sm:text-5xl">{t("Negocie com quem entende.")}</h2><p className="mt-5 leading-7 text-white/55">{t("Tem uma guitarra, violão, pedal ou amp parado? Envie os detalhes. A conversa continua direto no WhatsApp, sem formulários complicados.")}</p><div className="mt-8 space-y-4 text-sm text-white/70"><div className="flex gap-3"><ShieldCheck className="shrink-0 text-red-400" size={19}/><span>{t("Avaliação cuidadosa e transparente.")}</span></div><div className="flex gap-3"><MessageCircle className="shrink-0 text-red-400" size={19}/><span>{t("Resposta rápida pelo WhatsApp.")}</span></div><div className="flex gap-3"><MapPin className="shrink-0 text-red-400" size={19}/><span>{t("Atendimento em Cuiabá e região.")}</span></div></div></div><form onSubmit={submitTrade} className="rounded-3xl border border-white/10 bg-white/[.045] p-6 sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><div><Label>{t("Seu nome")}</Label><Input value={trade.name} onChange={e => setTrade({ ...trade, name: e.target.value })} placeholder={t("Como podemos te chamar?")} className="mt-2 border-white/10 bg-black/20 text-white placeholder:text-white/25"/></div><div><Label>{t("WhatsApp / contato")}</Label><Input value={trade.contact} onChange={e => setTrade({ ...trade, contact: e.target.value })} placeholder="(65) 99999-9999" className="mt-2 border-white/10 bg-black/20 text-white placeholder:text-white/25"/></div></div><div className="mt-5"><Label>{t("Instrumento para negociar")}</Label><Input value={trade.instrument} onChange={e => setTrade({ ...trade, instrument: e.target.value })} placeholder={t("Ex.: Fender Stratocaster 2018")} className="mt-2 border-white/10 bg-black/20 text-white placeholder:text-white/25"/></div><div className="mt-5"><Label>{t("Conte um pouco mais")}</Label><Textarea value={trade.message} onChange={e => setTrade({ ...trade, message: e.target.value })} placeholder={t("Estado, acessórios, se aceita troca...")} className="mt-2 min-h-28 border-white/10 bg-black/20 text-white placeholder:text-white/25"/></div><Button type="submit" className="mt-6 h-12 w-full rounded-xl bg-red-600 hover:bg-red-500">{t("Enviar para o WhatsApp")} <MessageCircle size={17}/></Button></form></div></section>
    </main>

    <footer id="contato" className="border-t border-white/10 bg-black/20"><div className="container grid gap-10 py-14 md:grid-cols-[1.2fr_1fr_1fr_1fr]"><div><div className="flex items-center gap-3"><ToneLogo language={language} size="footer" /></div><p className="mt-5 max-w-xs text-sm leading-6 text-white/45">{t("Instrumentos, equipamentos e conexões para quem leva música a sério.")}</p></div><div><p className="font-bold">{t("Visite a loja")}</p><p className="mt-4 flex gap-2 text-sm text-white/50"><MapPin size={17} className="text-red-400"/> {t("Cuiabá, Mato Grosso")}</p><p className="mt-2 text-sm text-white/50">{t("Seg a Sex • 09h às 18h")}</p></div><div><p className="font-bold">{language === "en" ? "International buyers" : "Compradores internacionais"}</p><Link href="/envio-internacional" className="mt-4 block text-sm text-white/50 hover:text-white">{language === "en" ? "Shipping guide" : "Guia de envio"}</Link><Link href="/confianca" className="mt-3 block text-sm text-white/50 hover:text-white">{language === "en" ? "Trust standard" : "Padrão de confiança"}</Link></div><div><p className="font-bold">{t("Fale com a Tone")}</p><a className="mt-4 flex gap-2 text-sm text-white/50 hover:text-white" href="https://instagram.com/tonemarketgt" target="_blank" rel="noreferrer"><Instagram size={17} className="text-red-400"/> @tonemarketgt</a><button onClick={() => whatsapp(t("Olá! Quero falar com a Tone Marketing."))} className="mt-3 flex gap-2 text-sm text-white/50 hover:text-white"><MessageCircle size={17} className="text-red-400"/> WhatsApp</button></div></div><div className="container border-t border-white/10 py-5 text-xs text-white/30">© {new Date().getFullYear()} Tone Marketing. Cuiabá • MT.</div></footer>
  </div>;
}
