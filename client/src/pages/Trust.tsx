import React from "react";
import { Link } from "wouter";
import { ArrowLeft, Camera, CheckCircle2, ClipboardCheck, PlayCircle } from "lucide-react";
import { LanguageToggle, useLanguage } from "@/lib/i18n";

export default function Trust() {
  const { language } = useLanguage();
  const english = language === "en";
  const inspection = english ? ["Overall condition and finish", "Neck, frets, hardware and electronics", "Included case, accessories and available invoice", "Description notes for relevant wear or modifications"] : ["Condição geral e acabamento", "Braço, trastes, ferragens e eletrônica", "Case, acessórios e nota fiscal disponível", "Observações sobre marcas de uso ou modificações relevantes"];
  const photos = english ? ["Full front and back", "Headstock and serial area", "Neck, frets and fingerboard", "Hardware, controls and case", "Any relevant marks in close detail"] : ["Frente e verso completos", "Headstock e área de serial", "Braço, trastes e escala", "Ferragens, controles e case", "Marcas relevantes em detalhe"];
  return <div className="min-h-screen bg-[#0b0b0c] text-white"><header className="border-b border-white/10"><div className="container flex h-20 items-center justify-between"><Link href="/" className="flex items-center gap-2 text-sm text-white/60 hover:text-white"><ArrowLeft size={17}/>{english ? "Back to store" : "Voltar à loja"}</Link><div className="flex items-center gap-3"><LanguageToggle/><img src="/manus-storage/tone-market-logo_77240e37.jpg" alt="Tone Market" className="h-10 w-14 rounded-lg object-cover"/></div></div></header><main className="container py-16 lg:py-24"><div className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-red-400">{english ? "Tone standard" : "Padrão Tone"}</p><h1 className="mt-5 text-5xl font-black tracking-[-.05em] sm:text-7xl">{english ? "More context. Better conversations." : "Mais contexto. Conversas melhores."}</h1><p className="mt-6 text-lg leading-8 text-white/60">{english ? "For remote buyers, the goal is not to replace a conversation—it is to make that conversation more transparent. Listings can be supported by technical notes, detailed imagery and instrument videos when they are available." : "Para compradores à distância, o objetivo não é substituir a conversa — é torná-la mais transparente. Os anúncios podem contar com ficha técnica, imagens detalhadas e vídeos do instrumento quando disponíveis."}</p></div><div className="mt-14 grid gap-6 lg:grid-cols-2"><section className="rounded-3xl border border-white/10 bg-white/[.035] p-7"><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.22em] text-red-300"><ClipboardCheck size={16}/>{english ? "Inspection notes" : "Notas de inspeção"}</p><ul className="mt-6 space-y-4">{inspection.map(item => <li key={item} className="flex gap-3 text-sm leading-6 text-white/65"><CheckCircle2 className="shrink-0 text-red-400" size={18}/>{item}</li>)}</ul></section><section className="rounded-3xl border border-white/10 bg-white/[.035] p-7"><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.22em] text-red-300"><Camera size={16}/>{english ? "Detailed photo guide" : "Guia de fotos detalhadas"}</p><ul className="mt-6 space-y-4">{photos.map(item => <li key={item} className="flex gap-3 text-sm leading-6 text-white/65"><CheckCircle2 className="shrink-0 text-red-400" size={18}/>{item}</li>)}</ul></section></div><section className="mt-6 rounded-3xl border border-red-500/20 bg-red-500/[.04] p-7"><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.22em] text-red-300"><PlayCircle size={16}/>{english ? "Video and customer content" : "Vídeo e conteúdo de clientes"}</p><p className="mt-4 max-w-3xl leading-7 text-white/60">{english ? "Instrument demos are presented separately from customer sessions. Customer performance videos are not used as synthetic reviews: they are shown only when the customer authorizes publication." : "Demonstrações de instrumentos são apresentadas separadamente das sessões de clientes. Vídeos de clientes não são tratados como avaliações criadas pela loja: eles só aparecem quando há autorização para publicação."}</p></section></main></div>;
}
